# 🎨 Инструкция по работе с Blender для настройки зон

## 📋 Быстрый процесс (3 шага)

### Шаг 1: Создайте маркеры зон в Blender

**Вариант A: Автоматическое создание (быстро)**

1. Откройте вашу модель в Blender
2. Перейдите на вкладку **Scripting** (вверху)
3. Нажмите **Open** и выберите `blender_create_sample_zones.py`
4. Нажмите **Run Script** ▶️
5. В сцене появятся 7 пустых объектов (Empty) с названиями `ZONE_*`
6. **Переместите их** в центры ваших залов:
   - Выберите Empty объект
   - Нажмите `G` (Grab/Move)
   - Двигайте мышью или введите координаты

**Вариант B: Создание вручную**

1. В 3D окне: `Shift + A` → Empty → Plain Axes
2. Переместите в центр зала (`G` для перемещения)
3. Переименуйте (`F2`): `ZONE_MainHall`, `ZONE_HallA`, и т.д.
4. Повторите для каждой зоны

### Шаг 2: Экспортируйте координаты

1. В Blender перейдите на вкладку **Scripting**
2. Нажмите **Open** и выберите `blender_extract_zones.py`
3. Нажмите **Run Script** ▶️
4. Скрипт создаст файл `zone_coordinates.json` в папке с вашей моделью
5. **В консоли Blender** (Window → Toggle System Console) появится готовый код для копирования

### Шаг 3: Примените координаты в приложении

**Автоматически:**
- Пришлите мне файл `zone_coordinates.json`
- Я обновлю приложение автоматически

**Вручную:**
- Скопируйте JavaScript код из консоли Blender
- Вставьте в `forum-nav-app/src/data/mockData.ts`

---

## 📐 Система координат

**Важно понимать:**

```
Blender:          Приложение:
X → право         X → право
Y → вперёд        Z → вперёд  
Z → вверх         Y → вверх

Преобразование:
App_X = Blender_X
App_Y = Blender_Z
App_Z = Blender_Y
```

Скрипт автоматически конвертирует координаты! ✅

---

## 🎯 Советы по размещению зон

### Где размещать Empty объекты:

1. **Конференц-залы** - центр зала на уровне пола
2. **Выставка** - центр выставочной площади
3. **Фуд-корт** - в центре зоны питания
4. **Регистрация** - перед стойкой регистрации
5. **Лаунж** - центр зоны отдыха

### Рекомендации:

- ✅ Размещайте на **уровне пола** (Z = 0 или близко к нулю)
- ✅ В **геометрическом центре** помещения
- ✅ Доступном месте (не в стене!)
- ✅ На **проходе** где могут стоять люди

---

## 🔍 Проверка координат

После создания Empty объектов:

1. Выберите Empty
2. Нажмите `N` → откроется боковая панель
3. Вкладка **Item** → **Transform** → **Location**
4. Запишите координаты X, Y, Z

---

## 📝 Формат названий зон

Используйте префикс `ZONE_` для автоматического определения:

```
ZONE_MainHall          ← Главный зал
ZONE_HallA             ← Зал А
ZONE_HallB             ← Зал Б
ZONE_Exhibition        ← Выставка
ZONE_FoodCourt         ← Фуд-корт
ZONE_Registration      ← Регистрация
ZONE_Lounge            ← Лаунж зона
ZONE_VIPRoom           ← VIP комната
ZONE_PressCenter       ← Пресс-центр
```

Скрипт автоматически:
- Уберёт префикс `ZONE_`
- Заменит `_` на пробелы
- Создаст читаемые названия

---

## 🎨 Дополнительные параметры

### Добавление описания зоны:

1. Выберите Empty объект
2. Нажмите `N` → вкладка **Custom Properties**
3. Добавьте свойства:
   - `description` - описание зоны
   - `color` - цвет в формате #RRGGBB
   - `type` - тип (conference, exhibition, food, etc.)
   - `capacity` - вместимость

---

## 🚀 Полный рабочий процесс

```
1. Откройте модель в Blender
   ↓
2. Запустите blender_create_sample_zones.py
   ↓
3. Настройте позиции Empty объектов вручную
   ↓
4. Запустите blender_extract_zones.py
   ↓
5. Пришлите мне zone_coordinates.json
   ↓
6. Я обновлю приложение
   ↓
7. Перезапустите npm run dev
   ↓
8. ✅ Зоны появятся на карте!
```

---

## ⌨️ Горячие клавиши Blender

| Клавиша | Действие |
|---------|----------|
| `Shift + A` | Добавить объект |
| `G` | Переместить |
| `R` | Поворот |
| `S` | Масштаб |
| `F2` | Переименовать |
| `N` | Боковая панель |
| `Numpad 7` | Вид сверху |
| `Numpad 1` | Вид спереди |
| `Numpad 3` | Вид сбоку |

---

## 📊 Пример вывода скрипта

После запуска `blender_extract_zones.py` вы увидите:

```
Found: ZONE_MainHall at (0.000, 0.000, 0.000)
Found: ZONE_HallA at (-20.500, 15.300, 0.000)
Found: ZONE_HallB at (20.500, 15.300, 0.000)
...
✅ SUCCESS! Found 7 Empty objects
📄 Saved to: C:\Users\akmal\Desktop\zone_coordinates.json
```

---

## ❓ Часто задаваемые вопросы

**Q: Сколько зон можно создать?**  
A: Сколько угодно! Скрипт найдёт все Empty объекты с префиксом ZONE_

**Q: Можно ли создавать зоны на разных этажах?**  
A: Да! Просто измените координату Z (высоту)

**Q: Что если я изменю позицию зоны?**  
A: Просто запустите скрипт экспорта снова - он перезапишет координаты

**Q: Нужно ли сохранять .blend файл?**  
A: Нет, скрипт работает с текущей сценой. Но лучше сохранить для дальнейших правок.

---

## 📞 Поддержка

После экспорта координат просто пришлите мне:
1. Файл `zone_coordinates.json`
2. (Опционально) Скриншот модели сверху

Я настрою всё остальное! 🚀
