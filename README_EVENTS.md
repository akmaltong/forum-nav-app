# 📅 Система управления мероприятиями

## Быстрый старт для заказчика

### Вариант 1: Excel/CSV (рекомендуется)

1. **Откройте файл `events_example.csv`** - это готовый пример
2. **Отредактируйте** данные под ваше расписание
3. **Сохраните** файл
4. **Отправьте** разработчику

Разработчик выполнит команду:
```bash
node convert_events_csv.js events_example.csv
```

И скопирует результат в приложение.

---

## Формат CSV файла

```csv
id,title,description,date,startTime,endTime,zoneId,speaker,capacity
event-1,Открытие форума,Торжественное открытие,2024-03-15,09:00,10:00,zone-1,Иванов И.И.,500
event-2,Мастер-класс,Практический мастер-класс,2024-03-15,10:30,12:00,zone-2,Петрова А.С.,50
```

### Описание полей:

| Поле | Обязательно | Пример | Описание |
|------|-------------|--------|----------|
| **id** | ✅ Да | event-1 | Уникальный номер (event-1, event-2...) |
| **title** | ✅ Да | Открытие форума | Название мероприятия |
| **description** | ✅ Да | Торжественное открытие | Краткое описание |
| **date** | ✅ Да | 2024-03-15 | Дата (ГГГГ-ММ-ДД) |
| **startTime** | ✅ Да | 09:00 | Время начала (ЧЧ:ММ) |
| **endTime** | ✅ Да | 10:00 | Время окончания (ЧЧ:ММ) |
| **zoneId** | ✅ Да | zone-1 | Номер зала |
| **speaker** | ❌ Нет | Иванов И.И. | Имя спикера |
| **capacity** | ❌ Нет | 500 | Вместимость зала |

---

## Список залов (zoneId)

Проверьте актуальный список в файле `src/data/mockData.ts`.

Типичные залы:
- `zone-1` - VIP-зал
- `zone-2` - Аккредитация
- `zone-3` - Арт-объект
- `zone-4` - Пресс-подход 1
- `zone-5` - Зона питания
- `zone-6` - Конференц-зал А
- `zone-7` - Конференц-зал Б

**⚠️ Важно:** zoneId должен существовать в системе! Уточните список у разработчика.

---

## Инструкция для разработчика

### 1. Получите CSV файл от заказчика

### 2. Конвертируйте CSV в TypeScript:

```bash
node convert_events_csv.js events.csv
```

Скрипт выведет готовый TypeScript код.

### 3. Откройте файл `src/data/mockData.ts`

### 4. Найдите массив `events`:

```typescript
export const events: Event[] = [
  // Здесь старые мероприятия
]
```

### 5. Замените содержимое на новый код

### 6. Сохраните файл

Приложение автоматически перезагрузится с новыми мероприятиями.

---

## Проверка после импорта

1. ✅ Откройте приложение: `npm run dev`
2. ✅ Откройте панель "Расписание" (иконка календаря)
3. ✅ Проверьте, что все мероприятия отображаются
4. ✅ Откройте панель "Зоны" (иконка карты)
5. ✅ Убедитесь, что мероприятия привязаны к правильным зонам
6. ✅ Проверьте время начала и окончания
7. ✅ Проверьте, что кнопки "Избранное" и "Напоминание" работают

---

## Примеры использования

### Пример 1: Простое мероприятие

```csv
event-1,Регистрация,Получение бейджей,2024-03-15,08:00,09:00,zone-2,,300
```

### Пример 2: Мероприятие со спикером

```csv
event-2,Открытие форума,Торжественное открытие,2024-03-15,09:00,10:00,zone-1,Иванов И.И.,500
```

### Пример 3: Параллельные секции

```csv
event-5,Секция А,Инновации,2024-03-15,12:00,13:30,zone-6,Петров П.П.,100
event-6,Секция Б,Маркетинг,2024-03-15,12:00,13:30,zone-7,Сидорова С.С.,80
```

---

## Файлы в проекте

- **`events_example.csv`** - Готовый пример расписания на день
- **`convert_events_csv.js`** - Скрипт конвертации CSV → TypeScript
- **`import_events.js`** - Альтернативный способ через JavaScript
- **`EVENTS_IMPORT_GUIDE.md`** - Подробное руководство
- **`ИНСТРУКЦИЯ_ДЛЯ_ЗАКАЗЧИКА.md`** - Простая инструкция для заказчика
- **`README_EVENTS.md`** - Этот файл

---

## Частые вопросы

**Q: Можно ли добавить мероприятия на несколько дней?**  
A: Да, просто используйте разные даты в колонке `date`.

**Q: Что если зал не существует?**  
A: Мероприятие не отобразится корректно. Сначала добавьте зал в `zones` массив.

**Q: Можно ли изменить время мероприятия?**  
A: Да, просто отредактируйте CSV и запустите конвертацию заново.

**Q: Как удалить мероприятие?**  
A: Удалите строку из CSV или из массива `events` в `mockData.ts`.

**Q: Поддерживаются ли повторяющиеся мероприятия?**  
A: Нет, каждое мероприятие нужно добавлять отдельно.

---

## Поддержка

Если возникли проблемы:
1. Проверьте формат CSV (запятые, кавычки)
2. Убедитесь, что все обязательные поля заполнены
3. Проверьте, что zoneId существует
4. Откройте консоль браузера (F12) для просмотра ошибок
5. Обратитесь к разработчику

---

## Технические детали

### Структура Event в TypeScript:

```typescript
interface Event {
  id: string
  title: string
  description: string
  startTime: string  // ISO 8601 format
  endTime: string    // ISO 8601 format
  zoneId: string
  speaker?: string
  capacity?: number
}
```

### Где хранятся данные:

- **Зоны**: `src/data/mockData.ts` → `zones` массив
- **Мероприятия**: `src/data/mockData.ts` → `events` массив
- **POI (точки интереса)**: `src/data/poiData.ts` → `pois` массив

---

## Changelog

- **v1.0** - Первая версия системы импорта
- Добавлена поддержка CSV
- Добавлены скрипты конвертации
- Добавлена документация
